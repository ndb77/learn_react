import React from 'react';
import ReactDOM from 'react-dom'; // a package within the package.json
import './index.css';
import App from './App';

// render the custom HTML element onto the root element
ReactDOM.render(<App/>, document.getElementById('root'));
